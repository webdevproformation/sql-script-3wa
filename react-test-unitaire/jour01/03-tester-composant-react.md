# créer notre premier composant  `Question.tsx`

- pour les composants 
- utiliser plusieurs fonctions qui permettent de simuler le DOM
- screen => sélectionner une balise 
- render => exécuter le composant dans un environnement DOM virtuel


- déclencher des evénements dans notre composant comme le fait de cliquer sur un bonjour
- le faite de modifier le contenu d'un input 

```sh
npm install -D @testing-library/user-event
```

cette librairie permet de mettre à disposition un objet `userEvent` qui simule des actions sur les éléments d'une composant 
 